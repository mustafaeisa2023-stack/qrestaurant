[build]
builder = "NIXPACKS"
buildCommand = "npm run build && npx prisma generate"

[deploy]
startCommand = "npx prisma migrate deploy && npx prisma generate && node dist/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3