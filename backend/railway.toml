[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate && npm run build && ls dist/"

[deploy]
startCommand = "npx prisma migrate deploy && node dist/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3